import 'package:flutter/material.dart';
import 'item/dealItem.dart';

class Deals extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: dealItem(),
    );
  }
}
